# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# LLM — Groq (OpenAI-compatible, free tier)
groq>=0.9.0

# Embeddings — local, no API key, runs on CPU
sentence-transformers>=3.0.0

# Vector search — in-memory FAISS
faiss-cpu>=1.8.0

# BM25 lexical retrieval
rank-bm25>=0.2.2

# PDF parsing — lightweight, no native deps
pypdf>=5.0.0

# Data & utilities
numpy>=1.26.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0